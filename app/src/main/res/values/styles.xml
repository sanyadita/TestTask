<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="imageButtonStyle">@style/AppTheme.ImageButton</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.ImageButton" parent="Widget.AppCompat.ImageButton">
        <item name="android:background">@drawable/btn_ripple_background</item>
        <item name="android:padding">@dimen/view_margin_8dp</item>
        <item name="android:tint">@color/colorIconButton</item>
    </style>

    <style name="AppTheme.ImageButton.IsFavourite" parent="AppTheme.ImageButton">
        <item name="android:background">@drawable/btn_ripple_background_accent</item>
        <item name="android:tint">@color/colorAccent</item>
    </style>

</resources>
